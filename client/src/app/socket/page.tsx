"use client";

import useSocket from "@ai/utils/hooks/use-socket";
import SocketInitializer from "@ai/utils/hooks/use-socket";

export default async function Socket() {
  // const { players } = useSocket();

  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center">
      <p>Hello</p>
    </main>
  );
}

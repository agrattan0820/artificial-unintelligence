{
  "name": "database",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "prettier . --check --experimental-cli && eslint .",
    "db:migrate:run": "tsx --env-file-if-exists=.env migrate.ts",
    "db:seed": "tsx --env-file-if-exists=.env seed.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.8"
  },
  "devDependencies": {
    "@auth/core": "^0.40.0",
    "@types/node": "^25.5.0",
    "drizzle-kit": "^0.31.10",
    "eslint": "^9.39.4",
    "eslint-config-custom": "workspace:*",
    "prettier": "^3.8.1",
    "tsconfig": "workspace:*",
    "tsx": "^4.21.0",
    "typescript": "5.9.3"
  }
}

import ErrorScreen from "@ai/components/error-screen";

export default function SignInLandingPage() {
  return (
    <ErrorScreen details="Choose a nickname on the homepage before signing in with Google." />
  );
}
